package brush.algorithms4.a_base;

/**
 * @author: lazecoding
 * @date: 2021/2/1 21:58
 * @description:
 */
public class ApiExamples {

    /**
     * 数学函数
     */
    public void MathApi(){
        int a = 1;
        int b = 2;

        Math.abs(a);            // a 的绝对值
        Math.max(a, b);         // a 和 b 中的较大者
        Math.min(a, b);         // a 和 b 中的较小者


        Math.sin(a);            // a 的正弦函数
        Math.cos(a);            // a 的余弦函数
        Math.tan(a);            // a 的正切函数

        Math.exp(a);            // a 的正弦函数
        Math.log(a);            // a 的余弦函数
        Math.pow(a, b);         // a 的正切函数
        Math.random();          // a 的正切函数
        Math.sqrt(a);           // a 的正切函数


        double e = Math.E;      // 常数 E
        double pi = Math.PI;    // 常数 π
    }

    public static void main(String[] args) {


    }
}